{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "default",
  "description": "Capability set for the HQ Work mobile webview. Deliberately narrow: OS identification for platform resolution, notifications, HTTP, and the two permissions sign-in needs (opening the Cognito hosted UI in the system browser, and receiving the hqmobile:// callback). No filesystem, shell, tray or window-management permissions, because MOBILE_CAPABILITIES declares all of those false.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "os:default",
    "notification:default",
    "http:default",
    "deep-link:default",
    "opener:allow-open-url"
  ]
}
