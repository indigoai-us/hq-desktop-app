{
  "name": "@hq/intro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone preview of the HQ cinematic welcome intro. Opens full-screen over the desktop, plays the film, quits. No sign-in, no sync, no tray.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.4.8",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
