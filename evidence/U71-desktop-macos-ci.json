{
  "unit": "U71",
  "programme": "realtime HQ vault sync",
  "kind": "ci-gate-evidence",
  "sourceSeed": "no product edits; CI status/digest evidence",
  "dormant": true,
  "workflow": {
    "path": ".github/workflows/ci.yml",
    "sha256": "8d4e287fef3cb0190b425b5a41dc092c1cf05c37ff9e05860679790e170c59cf",
    "job": "Rust tests (macOS)",
    "runner": "macos-latest",
    "appleTargets": [
      "aarch64-apple-darwin"
    ]
  },
  "requiredPreparation": [
    "pnpm install --no-frozen-lockfile",
    "apps/sync: pnpm run sidecar:install",
    "apps/sync: pnpm build"
  ],
  "requiredBlockingCommands": [
    "cargo test --workspace --locked",
    "apps/sync/src-tauri: cargo test --locked",
    "apps/sync/src-tauri: cargo test --locked commands::process::cross_generation_escalation_tests -- --nocapture",
    "cargo check --workspace --locked",
    "apps/sync/src-tauri: cargo check --locked"
  ],
  "passCondition": "The pull request's Rust tests (macOS) check succeeds for this evidence commit; a failed or absent check blocks deploy."
}
