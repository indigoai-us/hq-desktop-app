/**
 * White-label brand slot (US-005), owned by @hq/ui so both the menubar
 * popover and the desktop shell render the same lockup from the same runtime.
 */
export { default as BrandLogoSlot } from './BrandLogoSlot.svelte';
export { default as PoweredByHq } from './PoweredByHq.svelte';
export * from './brand.js';
